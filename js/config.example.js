/* ═══════════════════════════════════════════════════
   config.example.js — Template for EmailJS Credentials

   Copy this file to config.js and fill in your values.
   config.js is git-ignored and will NOT be committed.

   1. emailjs.com → Email Services → copy SERVICE_ID
   2. Email Templates → notification template → copy TEMPLATE_ID
   3. Email Templates → auto-reply template → copy AUTOREPLY_ID
   4. Account → General → copy PUBLIC_KEY
   ════════════════════════════════════════════════════ */

var EMAILJS_CONFIG = {
    SERVICE_ID:    'YOUR_SERVICE_ID',
    TEMPLATE_ID:   'YOUR_TEMPLATE_ID',
    AUTOREPLY_ID:  'YOUR_AUTOREPLY_ID',
    PUBLIC_KEY:    'YOUR_PUBLIC_KEY'
};
